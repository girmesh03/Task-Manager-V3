import { Card, CardContent } from "@mui/material";

const LeaderBoard = () => {
  return (
    <Card variant="outlined" sx={{ maxHeight: 327, overflow: "auto" }}>
      <CardContent>LeaderBoard</CardContent>
    </Card>
  );
};

export default LeaderBoard;
